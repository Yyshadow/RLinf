# Probe QAM with the H50, tau=0.9 critic selected from the critic ablation.

defaults:
  - robotwin_beat_block_hammer_lwd_qam_openpi_pi05
  - _self_

runner:
  logger:
    experiment_name: robotwin_beat_block_hammer_lwd_qam_openpi_pi05_allstats_probe_h50_tau09_lq005_gc005
  max_steps: 300
  save_interval: 100

algorithm:
  lambda_q: 0.05
  qam_grad_clip: 0.05
  qam_compare_interval: 5

critic:
  model:
    action_horizon: 50
    quantile_tau: 0.9
    model_path: ${oc.env:RLINF_QAM_CRITIC_MODEL_PATH,/mnt/dolphinfs/hdd_pool/docker/user/hadoop-uavcvml/yangyi122/checkpoints/rlinf_lwd_critic_test/robotwin_lwd_critic_s1_f1_n1_h50_tau09_8a100/checkpoints/global_step_8000/actor}
